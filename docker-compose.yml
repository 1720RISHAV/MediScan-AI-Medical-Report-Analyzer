version: '3.8'

services:
  mediscan-ai:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./mediscan.db:/app/mediscan.db
    restart: unless-stopped